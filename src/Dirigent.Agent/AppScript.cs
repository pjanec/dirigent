namespace Dirigent.Agent
{
	/// <summary>
	/// Script associated with the app (optional)
	/// Can calculate app status, control the application launching/killing etc.
	/// Receives the app control commands (LaunchApp, KillApp, RestartApp)
	/// Ticked each frame.
	/// </summary>
	public class AppScript
	{
		// TODO: implement as a watcher?
	}
}
